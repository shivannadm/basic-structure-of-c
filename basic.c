#include <stdio.h> // This is the preprossesive directives , Header file

int main() // int is the keyword , Main function starts here
{
    int a = 10;                        // a is a variable
    printf("The value of a is %d", a); // displaying the a value
    return 0;                          // returning the value
}
